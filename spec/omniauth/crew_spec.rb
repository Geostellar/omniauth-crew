require "spec_helper"

RSpec.describe Omniauth::Crew do
  it "has a version number" do
    expect(Omniauth::Crew::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
